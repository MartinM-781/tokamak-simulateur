{
  "name": "tokamak-simulateur",
  "version": "0.1.0",
  "private": true,
  "description": "Suite pédagogique tokamak + générateur de datasets synthétiques de disruptions",
  "scripts": {
    "test": "node --test tests/"
  }
}
