[project]
name = "tokamak-ml"
version = "0.1.0"
description = "Baseline ML de prédiction de disruptions sur les datasets synthétiques tokamak v5"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "scikit-learn>=1.3",
    "matplotlib>=3.7",
]
